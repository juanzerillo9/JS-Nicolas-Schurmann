// Una funcion se pueden escribir de dos formas

function NombreFuncion(argumento) {
    console.log(argumento);
}

NombreFuncion('Jose Luiiiiiiiiiis');

function iterar (arr){
    for ( let i = 0 ; i < arr.lenght ; i++ ){
        console.log(arr[i]);
    }
}

const Arreglo = [1, 4, 'Hola', 'Mundo', true, 54, 'Chanchito Feliz'];

iterar(Arreglo);